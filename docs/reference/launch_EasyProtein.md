# Launch EasyProtein Shiny App

Launch the EasyProtein interactive Shiny application.

## Usage

``` r
launch_EasyProtein(install_missing = TRUE, ...)
```

## Arguments

- install_missing:

  Logical. Whether to install missing packages automatically.

- ...:

  Passed to shiny::runApp().
