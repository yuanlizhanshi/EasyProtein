# Scale matrix by row

Scale each row of a numeric matrix to mean 0 and sd 1.

## Usage

``` r
scale_mtx(mtx)
```

## Arguments

- mtx:

  A numeric matrix.

## Value

A scaled matrix with the same dimensions.
