% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/QC.R
\name{calc_group_median_gene_cv_from_exp_file}
\alias{calc_group_median_gene_cv_from_exp_file}
\title{Calculate median gene CV within each raw-data group}
\usage{
calc_group_median_gene_cv_from_exp_file(
  exp_file,
  obs_col = "Genes",
  raw_prefix = "auto",
  group_by = "condition"
)
}
\arguments{
\item{exp_file}{Path to the raw expression table file.}

\item{obs_col}{Name of the column in \code{exp_file} used to define feature
identifiers. Only the substring before the first semicolon (\code{;}) will
be used as the feature name.}

\item{raw_prefix}{Character string used to identify raw expression columns.
If \code{"auto"} (default), raw intensity columns are detected using the
same filename-style suffix rules as \code{rawdata2se()}.}

\item{group_by}{Column in the parsed sample metadata used for grouping.
Default is \code{"condition"}.}
}
\value{
A data.frame with one row per group, including the median gene CV
  and the 95th percentile CV across replicated samples in that group.
}
\description{
Read the same raw expression table accepted by \code{rawdata2se()}, parse
sample conditions using the same sample-name rule, calculate each gene's CV
across replicates within each condition, and return the median CV per
condition. Missing values are ignored.
}
